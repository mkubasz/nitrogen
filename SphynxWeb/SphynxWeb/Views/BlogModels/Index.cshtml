@model IEnumerable<Repozytorium.Models.BlogModel>
<link href="/Content/css/1-col-portfolio.css" rel="stylesheet">
@{
    ViewBag.Title = "Index";
    Styles.Render("~/Content/css");
}
@foreach (var item in Model)
{
    <div class="row">

        <div class="col-md-12">
            <h1>
                @Html.DisplayFor(modelItem => item.AddDate) - 
             @Html.DisplayFor(modelItem => item.Name)</h1>

            <div id="cos">
                <h4>
                    @Html.DisplayFor(modelItem => item.Post)
                    @*@item.Post*@
            <br> Czytaj wiecej</h4>


</div>
           
            @*<a class="btn btn-primary" href="#">View Project <span class="glyphicon glyphicon-chevron-right"></span></a>*@
        </div>
    </div>
}







@*<div class="service text-center">
    <div class="row">
        <div class="col-xs-2 col-sm-2 col-md-2">

        </div>
        <div class="col-xs-8 col-sm-8 col-md-8">
            <h1>Aktualności w budowie </h1>
        </div>
        <div class="col-xs-2 col-sm-2 col-md-2">

        </div>
    </div>

    @foreach (var item in Model)
    {
        <div class="container">
            <div class="row">

                <div class="col-xs-4 col-sm-4 col-md-4"> @Html.DisplayFor(modelItem => item.AddDate)</div>

                <div class="col-xs-8 col-sm-8 col-md-8"> @Html.DisplayFor(modelItem => item.Name)</div>
                <div class="row">
                    <div class="col-xs-12 col-sm-12 col-md-12">@Html.DisplayFor(modelItem => item.Post)</div>
                </div>
            </div>
        </div>

    }
</div>*@


        @*<table class="table">
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.AddDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Post)
                        </td>

                        @*   <td>
                                @Html.ActionLink("Edit", "Edit", new { id=item.PostId }) |
                                @Html.ActionLink("Details", "Details", new { id=item.PostId }) |
                                @Html.ActionLink("Delete", "Delete", new { id=item.PostId })
                            </td>
                    </tr>
                }

            </table>*@
